# Local-only files
.netlify/
.size-test.bin
test-sync.txt
node_modules/
deploy-*.zip
*.zip
desktop.ini
# Weirdly named artifact directory
{css,js,admin,sales}/

# Raw/reference assets not needed for deploy (huge .tif source files)
Product photos/

# Cursor AI tooling data, not project code
.cursor/skills/

# Local business documents, not meant for the public site repo
VitaTerra-Taxil-Qargidali-Uygunluq.xlsx
