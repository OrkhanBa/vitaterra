[build]
  publish = "."
  functions = "netlify/functions"

[build.processing]
  skip_processing = true

# /admin and /sales are SPA routes served by the single-file app.
# Rewrite (status 200) so the URL bar stays clean instead of redirecting.
[[redirects]]
  from = "/admin"
  to = "/index.html"
  status = 200

[[redirects]]
  from = "/admin/*"
  to = "/index.html"
  status = 200

[[redirects]]
  from = "/sales"
  to = "/index.html"
  status = 200

[[redirects]]
  from = "/sales/*"
  to = "/index.html"
  status = 200

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Content-Security-Policy = "script-src 'self' 'unsafe-inline' https://cdnjs.cloudflare.com https://*.clerk.accounts.dev https://clerk.vitaterra.az; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://fonts.gstatic.com; connect-src 'self' https://*.clerk.accounts.dev https://clerk.vitaterra.az; frame-src https://*.clerk.accounts.dev https://clerk.vitaterra.az;"

# Cache content files briefly so admin sees updates fast,
# but visitors don't hammer the server.
[[headers]]
  for = "/site-content.json"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

[[headers]]
  for = "/products.json"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

[[headers]]
  for = "/sales-users.json"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

[[headers]]
  for = "/clerk-config.json"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

# Force browsers to re-check index.html on every visit so deploys
# show up immediately on desktop and mobile (no stale HTML cache).
[[headers]]
  for = "/"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

[[headers]]
  for = "/index.html"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"
